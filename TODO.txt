TODO：今後の実装予定

----------------------------------------
チュートリアルが必要なのでwikiを作成

luadocが一部上手くいっていない。
エンコードとかファイルの説明とか。
カスタムdocletとtagletを作成するかどうか。

----------------------------------------
クラスのnamespaceについて
全てグローバル空間にテーブルを作成しているので、
hs名前空間を設定するような方法を検討中。

----------------------------------------
global関数の使用を廃止して、別の名称に置き換え

----------------------------------------
必要がなくなったテクスチャ、フォントの自動的な解放
現在では、全てテクスチャをキャッシュして、解放するタイミングがない。
シーン切り替え時に、不要なリソースを解放するような実装を追加するべき。

----------------------------------------
複数のScene間のHUDオブジェクト
これはいるかわからないが、追加すべきか。

----------------------------------------
DisplayObjectのVisibleの連携
visibleが正常に動作していなかった気がする。

----------------------------------------
モバイルの画面サイズの最適化
特にAndroidだと、画面サイズがまちまちなので、
対象としたいViewportのサイズを設定すれば、
近づけるようなロジックを設定したい。

----------------------------------------
TMXMapの強化
TMXObjectで任意のDisplayObjectを生成可能にする。
また、Box2DBodyも生成可能とする。
さらに、Factoryクラスによりカスタマイズを容易とする。

----------------------------------------
Box2Dの継続的な機能の強化
DisplayObjectから生成可能に機能を追加する。

----------------------------------------
GUI機能(ListBox,Button,RadioButton,MessageBox)
基本的なGUI部品を作成したい。

----------------------------------------
ゲーム用モジュール（RPG,SLG,AVG）
これはやっぱりいらないかも。

